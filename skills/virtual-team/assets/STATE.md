# STATE — resume authority (PM-owned; read first after compaction)

## Team
PM = main session. PL = <PL_NAME> (Claude teammate). QA = <QA_NAME> (Claude teammate). Designer = <DESIGNER_NAME> (codex). Dev = <FE_NAME> (codex).
Bible (owner-only, verbatim cards) = <repo>/intent/verbatim.md.

## Current sprint
Sprint <n>: <one line>. Minutes: meetings/planning-<n>.md (LOCKED, D<n>-1..k).

## Progress log (newest last; one paragraph per event; paths absolute or repo-relative)

## Lessons carried (rules adopted in retros)

## Deferred owner decisions
